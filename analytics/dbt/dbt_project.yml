name: pulseforge_analytics
version: 1.0.0
config-version: 2
profile: pulseforge

model-paths: ["models"]
test-paths: ["tests"]
target-path: target
clean-targets: [target, dbt_packages]

models:
  pulseforge_analytics:
    staging:
      +materialized: view
    dimensions:
      +materialized: table
    facts:
      +materialized: table
    marts:
      +materialized: table
